# STL Containers Project

C++ implementation of STL container tasks using std::array, std::vector, std::deque.
No STL algorithms used. All tasks implemented as template functions.

## Build
g++ -std=c++17 main.cpp -o stl_containers
